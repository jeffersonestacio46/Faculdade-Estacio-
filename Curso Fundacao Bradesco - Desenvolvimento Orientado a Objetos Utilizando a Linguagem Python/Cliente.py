class cliente: #Cliente é uma classe
    def __int__(self): #underline, underline, a palavra reservada init, underline, underline. O init() é um método especial que será chamado sempre que criarmos um objeto da classe.
        self.nome
        self.telefone

        pass #Usa quando não há nada declaro na classe